# MID TERM SUBMISSION:

## Execution Steps for *QUESTION 2*
1. Multi Client Chat System has been impented using * TCP and threading * we have used a data structure called list 
2. we followed the TCP Server steps of making an environment where there is one host server with a PORT number with 65,535 (qpprox choices) I chose 12345 as my port number 
3. Bind the server-> connect-> accept-> receive-> send-> close there are functions too which are self explainatory

## Execution Steps for *Question 3*
1. Imported cv2 module used cascades for face detection integrated it with mqtt (publisher and subscriber both)
2. 
### Attempted all the questions in Python